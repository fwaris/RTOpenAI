plan(
    "<plan title>",                            % Title
    category("all"),    
    lines([
      line(1, monthly_price(20), original_price(25)),
      line(2, monthly_price(40), original_price(26))
    ]),
   features(
      [
      ]
    )   
).
